---
paths:
  - '../frontend/**'
---

# Frontend 2

## Keep browser source in the frontend workspace
Blade, JavaScript, CSS, Vite configuration, and JavaScript tests belong under the sibling frontend/ directory. Vite must continue emitting deployable assets to ../backend/public/build.
