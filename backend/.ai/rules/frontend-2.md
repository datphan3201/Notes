---
paths:
  - '../frontend/**'
---

# Frontend 2

## Keep browser source stable
PHP views, JavaScript, CSS, Vite configuration, and JavaScript tests stay in frontend; Vite emits to ../backend/public/build. Use escaped PHP templates, preserve DOM hooks, same-origin URLs, JSON contracts, session cookies, and CSRF.
